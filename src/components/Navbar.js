import { Link } from "react-router-dom"
import { useLogout } from '../hooks/useLogout'

// styles

export default function Navbar() {
    const { logout } = useLogout()

    // TODO front: sign up, log in, log out
    return ()
}